import { Meta } from "@storybook/blocks";
import { Text } from "../components";
import { Flex } from "../generated/panda/jsx";

import { app } from "../lib/config";

<Meta title="Introduction" />

<Flex direction="column" alignItems="center" gap={4}>

<img src="/svg/logo.svg" width="124px" />

# Introduction

</Flex>

Welcome to the {app.organization.name} {app.name.short} Storybook! This Storybook outlines the {app.name.short} design system, as well as the components, hooks, and other elements available in the UI library and toolkit. This is a living style guide that provides a comprehensive overview of {app.name.short}'s design language and usage guidelines.

### Components

Components are implementations of the {app.name.short} design system. They are the building blocks of the UI library and toolkit, ready for use in applications.

The components are broken up into the following categories:

- **Core:** UI components used for common patterns and implementations.
- **Typography:** UI components used for text and typography.
- **Layout:** UI components used for page layout and structure.
