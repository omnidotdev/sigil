import { defineSemanticTokens } from "@pandacss/dev";

import type { SemanticTokens } from "@pandacss/dev";

/**
 * Blur semantic tokens.
 */
const blurs: SemanticTokens["blurs"] = defineSemanticTokens.blurs({});

export default blurs;
