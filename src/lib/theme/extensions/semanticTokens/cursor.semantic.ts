import { defineSemanticTokens } from "@pandacss/dev";

import type { Tokens } from "@pandacss/dev";

/**
 * Cursor semantic tokens.
 */
const cursor: Tokens["cursor"] = defineSemanticTokens.cursor({});

export default cursor;
