import { defineTokens } from "@pandacss/dev";

import type { Tokens } from "@pandacss/dev";

/**
 * Border width tokens.
 */
const borderWidths: Tokens["borderWidths"] = defineTokens.borderWidths({});

export default borderWidths;
