import { defineTokens } from "@pandacss/dev";

import type { Tokens } from "@pandacss/dev";

/**
 * Container name tokens.
 */
const containerNames: Tokens["containerNames"] = defineTokens.containerNames(
  {},
);

export default containerNames;
