import { defineTokens } from "@pandacss/dev";

import type { Tokens } from "@pandacss/dev";

/**
 * Cursor tokens.
 */
const cursor: Tokens["cursor"] = defineTokens.cursor({});

export default cursor;
