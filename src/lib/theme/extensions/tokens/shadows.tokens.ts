import { defineTokens } from "@pandacss/dev";

import type { Tokens } from "@pandacss/dev";

/**
 * Shadow tokens.
 */
const shadows: Tokens["shadows"] = defineTokens.shadows({});

export default shadows;
