---
"@omnidev/sigil": minor
---

**BREAKING:** Replace `displayClearTrigger` in `Combobox` and `Select` with `clearTrigger` React node render. This was done to maintain consistency with the rest of the Sigil components, e.g. `TagsInput`
