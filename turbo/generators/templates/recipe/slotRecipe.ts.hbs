import { defineSlotRecipe } from "@pandacss/dev";

/**
 * {{ pascalName }} recipe.
 */
const {{ camelName }} = defineSlotRecipe({
  className: "{{ camelName }}",
  description: "{{ pascalName }} style recipes",
  slots: [
    // TODO add slots here; if applicable, make sure to use and extend Ark anatomy slots (e.g. `{{ camelName }}Anatomy.extendWith("customSlot").keys()`)
  ],
  base: {
    // TODO add base styles here
  },
  defaultVariants: {
    // TODO add default variants here
  },
  variants: {
    // TODO add variants here
  },
});

export default {{ camelName }};
